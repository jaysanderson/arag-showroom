# Flagship launch videos

`<slug>.mp4` (H.264, 1280×720 or 1920×1080, faststart) and `<slug>.jpg` (poster) per product slug from `config/products.json`.
When present, the product page's hero shows the video; otherwise it falls back to the first showcase still.
Produced with HyperFrames in `../../videos/<product>-launch/` (see the workspace).
